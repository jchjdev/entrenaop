name: entrenaop_admin
description: "Panel web de administración de EntrenaOP"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  workout_core:
    path: ../packages/workout_core
  workout_editor_ui:
    path: ../packages/workout_editor_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
