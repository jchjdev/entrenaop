name: workout_editor_ui
description: "Controles compartidos para crear sesiones en EntrenaOP"
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image: ^4.10.1
  image_picker: ^1.2.3
  workout_core:
    path: ../workout_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
